# Serveur
PORT=3000

# PostgreSQL (doit matcher docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=exam_hub
DB_USER=exam_hub_user
DB_PASSWORD=changeme

# JWT
JWT_SECRET=change_this_secret_in_production
JWT_EXPIRES_IN=24h

# Bcrypt
BCRYPT_SALT_ROUNDS=10